<%= render 'shared/error_messages' %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :full_name %>
<%= f.text_field :full_name %>

<% if current_user.admin? %>
  <%= f.label :position %>
  <%= f.collection_select :position, Article::POSITIONS, :to_s, :to_s, :include_blank => true %>
<%end%>

<%= f.label :email %>
<%= f.text_field :email %>

<%= f.label :password %>
<%= f.password_field :password %>

<%= f.label :password_confirmation, "Confirmation" %>
<%= f.password_field :password_confirmation %>
