<%= render 'error_messages' %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :content %>
<%= f.text_area :content %>

<%= f.submit @button_title, class:"btn btn-large btn-primary" %>

